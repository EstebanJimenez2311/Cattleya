from django.db import models

# Models for estadisticas app will be added as needed
# Currently, all statistics are calculated in real-time from Noticia model
# to ensure data consistency and avoid cache invalidation issues
