// Archivo de fallback - Ya no se usa desde que tenemos backend Django funcional
// Este archivo se mantiene por compatibilidad pero el frontend ahora obtiene datos directamente de la API

window.CATTLEYA_FALLBACK_NEWS = [
  // Datos de respaldo en caso de que la API falle completamente
  // Normalmente vacío ya que el backend maneja toda la lógica de datos
];
